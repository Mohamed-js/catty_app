class AddNewMessage {
  String name;
  String time;
  int messageCount;
  String imgPath;
  String lastMessage;

  AddNewMessage({this.messageCount, this.imgPath, this.time, this.name, this.lastMessage});
}
